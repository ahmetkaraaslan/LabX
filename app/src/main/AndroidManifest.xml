<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kimyasal">

        <!-- =============================================================== -->
        <!-- ==  Manufacturer-specific fullscreen/cutout support tags     == -->
        <!-- =============================================================== -->
        <!-- General Notch Support (Huawei, etc.) -->
        <meta-data android:name="android.notch_support" android:value="true" />
        <!-- Oppo -->
        <meta-data android:name="oppo_support_fullscreen" android:value="true" />
        <!-- Vivo -->
        <meta-data android:name="vivo_notch_support" android:value="true"/>
        <!-- Xiaomi -->
        <meta-data android:name="notch.config" android:value="portrait|landscape" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Kimyasal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScenarioActivity"
            android:exported="false"
            android:label="Senaryo Modu"
            android:theme="@style/Theme.Kimyasal" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="Ayarlar"
            android:theme="@style/Theme.Kimyasal" />
        <activity
            android:name=".FreeModeActivity"
            android:exported="false"
            android:label="Serbest Mod"
            android:theme="@style/Theme.Kimyasal" />
        <activity
            android:name=".TestActivity"
            android:exported="false"
            android:label="Test Modu"
            android:theme="@style/Theme.Kimyasal" />
    </application>

</manifest>